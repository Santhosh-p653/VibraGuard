<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Automated Attendance for Rural Schools</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #f0f0f0;
      color: #333;
      text-align: center;
      margin: 0;
      padding: 0;
      transition: background-color 0.3s, color 0.3s;
    }

    .dark-mode {
      background-color: #121212;
      color: #f0f0f0;
    }

    .container {
      padding: 60px 20px;
    }

    h1 {
      font-size: 42px;
      margin-bottom: 10px;
    }

    h3 {
      font-size: 22px;
      margin-bottom: 40px;
    }

    .menu button {
      margin: 10px;
      padding: 15px 30px;
      font-size: 18px;
      border: none;
      border-radius: 8px;
      background-color: #0078D4;
      color: white;
      cursor: pointer;
      transition: background-color 0.2s;
    }

    .menu button:hover {
      background-color: #005a9e;
    }

    #toggle-theme {
      margin-top: 30px;
      padding: 10px 20px;
      font-size: 16px;
      border: none;
      border-radius: 6px;
      background-color: #333;
      color: white;
      cursor: pointer;
    }

    #toggle-theme:hover {
      background-color: #555;
    }

    .reading-toggle {
      margin-top: 40px;
    }

    .switch {
      position: relative;
      display: inline-block;
      width: 60px;
      height: 34px;
    }

    .switch input {
      opacity: 0;
      width: 0;
      height: 0;
    }

    .slider {
      position: absolute;
      cursor: pointer;
      top: 0; left: 0;
      right: 0; bottom: 0;
      background-color: #ccc;
      transition: .4s;
      border-radius: 34px;
    }

    .slider:before {
      position: absolute;
      content: "";
      height: 26px; width: 26px;
      left: 4px; bottom: 4px;
      background-color: white;
      transition: .4s;
      border-radius: 50%;
    }

    input:checked + .slider {
      background-color: #0078D4;
    }

    input:checked + .slider:before {
      transform: translateX(26px);
    }

    #reading-status {
      margin-top: 10px;
      font-size: 18px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Automated Attendance for Rural Schools</h1>
    <h3>Team Neuroninjas</h3>

    <div class="menu">
      <button onclick="location.href='confusion.html'">Confusion Matrix</button>
      <button onclick="location.href='scatter.html'">Scatter Plot</button>
      <button onclick="location.href='bar.html'">Bar Graph</button>
    </div>

    <button id="toggle-theme">🌗 Toggle Theme</button>

    <div class="reading-toggle">
      <h4>Reading ON/OFF</h4>
      <label class="switch">
        <input type="checkbox" id="reading-toggle">
        <span class="slider"></span>
      </label>
      <p id="reading-status">Reading is OFF</p>
    </div>
  </div>

  <script>
    // Theme toggle
    document.getElementById("toggle-theme").addEventListener("click", () => {
      document.body.classList.toggle("dark-mode");
    });

    // Reading toggle
    const readingToggle = document.getElementById("reading-toggle");
    const readingStatus = document.getElementById("reading-status");

    readingToggle.addEventListener("change", () => {
      if (readingToggle.checked) {
        readingStatus.textContent = "Reading is ON";
        console.log("Simulated: Reading started");
        // You can add a fetch() call here to trigger backend if needed
      } else {
        readingStatus.textContent = "Reading is OFF";
        console.log("Simulated: Reading stopped");
      }
    });
  </script>
</body>
</html>

